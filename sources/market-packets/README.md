# Market packets

Dated files here are generated, reviewable derivatives of `data/`, prepared for analysis. They are not authoritative observations and must not silently add facts or conclusions. Regenerate one with `npm run market:packet -- --date YYYY-MM-DD`.
